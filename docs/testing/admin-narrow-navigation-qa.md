# 后台窄屏导航与目录表格验收（2026-09-09）

## 范围

依据用户提供的工作台、行业文案截图，修正窄屏导航占位过高和状态/操作标签碎行。仅修改后台导航模板、后台 CSS/JS、目录列表模板及测试；报告、评分、发布规则、业务数据结构不变。

- CSS 视口不超过 1023px 时默认收起导航，使用原生按钮展开/收起，维护 `aria-expanded` 和 `aria-controls`。
- 桌面恢复侧栏，所有入口及带 CSRF 的 POST 退出表单保留。
- JS 不运行时按钮隐藏、完整导航可用。菜单不使用浏览器存储。
- Esc 关闭菜单并聚焦按钮；宽度切换在隐藏已聚焦控件前转移焦点。
- 目录状态和操作按钮不换行，长内容编号仍可换行；保留具名、可聚焦的表格滚动区域。

## 自动化证据

- 新增 Python 契约测试先运行：2 failed（缺少菜单按钮及列样式），修正后后台 UI、展示、编辑器三个文件合计 **51 passed in 37.74s**。
- 新增导航 JS 测试先运行：4 passed、2 failed；实现后 6 passed。
- 浏览器发现聚焦菜单按钮后切回桌面会丢失焦点。测试补充隐藏控件同步失焦行为后：5 passed、1 failed；将焦点转移移到隐藏之前后，全部 JS 文件合计 **52 passed，0 failed，0 skipped**。
- `git diff --check` 未发现空白错误；Git 提示 Windows 检出时可能转换 LF/CRLF。
- 本次未重跑整套 Python 测试；历史 2243 项通过结果属于本次修改之前，不能代替上述本次验证。

## 浏览器证据

- 本地预览保持 `127.0.0.1:62851`，使用已合并主工作目录代码；原 QA 数据库保留，不重新初始化。
- 内置浏览器 960px CSS 视口：导航收起高度 76px，页面 clientWidth/scrollWidth 为 945/945，工作台卡片可直接看到。
- 展开后全部导航组和退出可访问；切至 1440px 恢复 240px 侧栏。修正后的按钮到桌面切换，实际焦点为品牌链接。
- 内置浏览器随后发生连接超时；恢复临时视口后使用 Edge 完成列表验证。
- Edge 实测 764px CSS 视口：页面 756/756，表格约 710.6px，状态与操作按钮 computed `white-space: nowrap`，截图确认文字完整一行。
- Edge 临时尺寸受既有缩放影响，首次请求 320px 实际仅 160px（不属于本次 320px 验收结果）；校准后实际 innerWidth=320，页面 312/312，表格滚动区 280px、内容 640px，整页无横向溢出。
- 表格区域可获得键盘焦点，方向键产生内部横向滚动；320px 下菜单展开、Esc 收起及按钮焦点已实际确认。
- 两个浏览器的临时视口均已恢复。

## 限制

未读取或更改浏览器原生缩放百分比，本次尺寸证据不代表正式的原生 200% 缩放验收。真实手机、原生 HTTP 400 视觉/焦点、物理打印及生产上线仍沿用既有待验收边界。代码审查未要求修改业务逻辑。

## HTTP 400 补验范围澄清（2026-09-09）

- 旧记录中的“通用 400”混用了两条路径：规则编辑器的错误摘要由 `admin/rule_release_edit.html` 提供 `role="alert"`、焦点目标；独立错误页由 `security._render_error` 渲染 `admin/error.html`，该模板没有相同的自动聚焦摘要。两者不能互相替代验收。
- 找到无需新建或修改业务记录的只读独立错误页入口：`GET /admin/assets/labels?unit=INVALID` 在读取标签数据前触发参数校验；没有执行标签打印。
- 此次浏览器清单可见 Edge，但创建标签页和连接现有后台标签页均返回 `Browser is not available`，未取得该 HTTP 400 页面截图、DOM 或焦点证据。未为了补验修改应用路由、创建规则草稿或改动业务数据。
- 规则编辑器摘要的浏览器实际聚焦，以及独立 HTTP 400 页的真实渲染，继续分别标记为待验收；自动化契约不代表这两项浏览器验收通过。
- 本轮重新执行 `tests/test_validation_and_errors.py` 与 `tests/test_admin_editor_ui.py`：**15 passed in 10.90s**，退出码 0；测试使用独立临时目录 `D:/Codex干活/.verification-tmp/error-check-0909-b`。
